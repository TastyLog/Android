<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.TastyLog" parent="Base.Theme.TastyLog">
        <!-- API 31 이상에서 사용될 스플래시 스크린 설정 -->
        <item name="android:windowSplashScreenBackground">@color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/icon_spalsh_screen</item>
        <item name="android:windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.Material3.DayNight.NoActionBar</item>
    </style>

    <!-- 스플래시 스크린 이후 적용될 테마 -->
    <style name="Theme.TastyLog.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/white</item> <!-- 상태바 색상-->
        <item name="android:windowLightStatusBar">true</item>   <!-- 상태바 아이콘(true: 검정 / false: 흰색) -->
        <item name="android:windowBackground">@color/white</item> <!--Layout 기본 색상 하얀색-->
    </style>
</resources>

